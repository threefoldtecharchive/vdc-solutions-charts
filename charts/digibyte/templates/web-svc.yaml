apiVersion: v1
kind: Service
metadata:
  name: web-digibyte-node
  labels:
    {{- include "digibyte.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    {{- include "digibyte.selectorLabels" . | nindent 4 }}
