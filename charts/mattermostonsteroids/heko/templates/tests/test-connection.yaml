apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "heko.fullname" . }}-test-connection"
  labels:
    {{- include "heko.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "heko.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
