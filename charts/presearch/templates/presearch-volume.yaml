apiVersion: v1
kind: PersistentVolume
metadata:
  name: presearch-volume-{{ .Release.Name }}
  labels:
    type: local
spec:
  storageClassName: local-path
  capacity:
    storage: 60Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/presearch"
