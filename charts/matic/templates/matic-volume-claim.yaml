apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    backupType: {{ .Values.threefoldVdc.backup | quote }}
  name: matic-volume-claim-{{ .Release.Name }}
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
